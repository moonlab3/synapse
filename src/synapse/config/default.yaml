/**:
  ros__parameters:
    muscle_embodiment: "LIBERO_PANDA"
    # camera_config: "wrist_cam"
    # camera_topic: "/synapse/camera/image_raw"

synapse_bt_node:
  ros__parameters:
    bt_tick_frequency_hz: 50
    obs_buffer_window_size: 20
    brain_option: "MANUAL"
    muscle_option: "DUMMY"

manual_adapter:
  ros__parameters:
    robot_description: True
    description_name: "panda_description"
    eef_frame: "panda_hand"

isaac_node:
  ros__parameters:
    publish_rate_hz: 100
    usd_path: "/home/rog-sf/ws/synapse_ws/src/synapse/resources/OXE.usd"
    robot_name: "panda"
    robot_prim_path: "/World/franka_set/Franka/panda"
