synapse_bt_node:
  ros__parameters:
    bt_tick_frequency_hz: 50
    obs_buffer_window_size: 20
    brain_option: "MANUAL"
    muscle_option: "ISAAC"
    # muscle_option: "DUMMY"

manual_adapter:
  ros__parameters:
    muscle_embodiment: "LIBERO_PANDA" # dual_arm # single_arm  # humanoid

gr00t_adapter:
  ros__parameters:
    muscle_embodiment: "LIBERO_PANDA"

isaac_node:
  ros__parameters:
    publish_rate_hz: 100
    usd_path: "/home/rog-sf/vla/OXE.usd"
    robot_name: "panda"
    robot_prim_path: "/World/franka_set/Franka/panda"
    camera_config: "wrist_cam"
