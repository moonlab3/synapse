synapse_bt_node:
  ros__parameters:
    bt_tick_frequency_hz: 50
    obs_buffer_window_size: 20
    brain_option: "MANUAL"  # Options: "VLA", "RL", "IL"
    # muscle_option: "DUMMY"  # Placeholder for future muscle options
    muscle_option: "ISAAC"
    muscle_embodiment: "LIBERO_PANDA"  # Options: "LIBERO_PANDA", "BEHAVIOR_R1_PRO", etc. Used for muscle nodes that require embodiment-specific formatting
    checkpoint_path: "/path/to/checkpoint"  # Used for RL and VLA options


muscle_options:
  ros__parameters:
    dummy_muscle_node:
      publish_rate_hz: 10
        # [To Be Added]
        # e_stop_topic: "/emergency_stop"
        # inference_timeout_ms: 200
    isaac_node:
      publish_rate_hz: 100
      usd_path: "/home/rog-sf/vla/OXE.usd"
      robot_name: "panda"
      robot_prim_path: "/World/franka_set/Franka/panda"
    robot_node:
      publish_rate_hz: 10
      # [To Be Added]
      # e_stop_topic: "/emergency_stop"